import { getData, getRandomStatus } from './data';

console.log(getRandomStatus());
console.log(getData(10));

function App() {
  return (
    <div>
      <h1>AAAAAAAAA</h1>
    </div>
  );
}

export default App;
